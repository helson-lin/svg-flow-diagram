interface:
  display_name: "SVG Flow Diagram"
  short_description: "Create styled SVG or draw.io flowcharts with dashed animated connectors"
  default_prompt: "Use $svg-flow-diagram to create an Excalidraw-like SVG or draw.io flowchart with dashed animated flow connectors, grouped nodes, and offset edge labels."
